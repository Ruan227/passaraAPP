import React from 'react';
import { Start } from './Start';

const Game = () => {
  const handleOnStartGame = () => {
    // TODO: Lógica para iniciar o jogo
  };

  return <Start handleOnStartGame={handleOnStartGame} />;

};

export { Game };
